<!DOCTYPE html>
<html>
<head>
  <title>Realtime Bridge Vibration Data</title>
  <meta charset="UTF-8" />
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; background: #f9f9f9; }
    h2 { color: #333; }
    table { border-collapse: collapse; width: 100%; margin-top: 20px; }
    th, td { padding: 10px; border: 1px solid #ccc; text-align: center; }
    th { background-color: #e0e0e0; }
  </style>
</head>
<body>
  <h2>Realtime Bridge Vibration Data</h2>
  <table id="data-table">
    <thead>
      <tr>
        <th>Timestamp</th>
        <th>X</th>
        <th>Y</th>
        <th>Z</th>
        <th>Vibration</th>
      </tr>
    </thead>
    <tbody id="data-body">
      <!-- Realtime data will appear here -->
    </tbody>
  </table>

  <!-- Firebase SDKs -->
  <script src="https://www.gstatic.com/firebasejs/9.6.1/firebase-app-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.6.1/firebase-database-compat.js"></script>

  <script>
    // Firebase Configuration (YOUR DATA)
    const firebaseConfig = {
      apiKey: "AIzaSyBMBtPkoaI7s_fiollPhLbgexUsPA3STRw",
      authDomain: "bridgeguard-iot2.firebaseapp.com",
      databaseURL: "https://bridgeguard-iot2-default-rtdb.asia-southeast1.firebasedatabase.app",
      projectId: "bridgeguard-iot2",
      storageBucket: "bridgeguard-iot2.appspot.com",
      messagingSenderId: "YOUR_SENDER_ID",
      appId: "YOUR_APP_ID"
    };

    // Initialize Firebase
    firebase.initializeApp(firebaseConfig);
    const database = firebase.database();

    // Reference to your database path (adjust if needed)
    const dataRef = database.ref("adxl_history"); // Misal: "adxl_history" dari ESP32

    // Real-time listener
    dataRef.limitToLast(20).on("child_added", (snapshot) => {
      const data = snapshot.val();
      const row = document.createElement("tr");

      row.innerHTML = `
        <td>${data.timestamp || "N/A"}</td>
        <td>${data.x?.toFixed(2) || 0}</td>
        <td>${data.y?.toFixed(2) || 0}</td>
        <td>${data.z?.toFixed(2) || 0}</td>
        <td>${data.vibration === 1 ? "YES" : "NO"}</td>
      `;

      const tbody = document.getElementById("data-body");
      tbody.prepend(row);
    });
  </script>
</body>
</html>
